filetype plugin indent on  " redundant with vim-sensible, but required for tab overrides to work
set tabstop=2 shiftwidth=2 expandtab
set hidden
" override pep8
autocmd FileType python set tabstop=2 shiftwidth=2 expandtab
